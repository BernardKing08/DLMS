spring:
  datasource:
    url: jdbc:h2:mem:accountdb;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: root
    password: root
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
